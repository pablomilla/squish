node_modules
dist
.env
.env.local
*.local
.DS_Store

# Benchmark: your meals, your results — not the repo's business
bench/photos/
bench/manifest.json
bench/results-*.json
bench/report.md

# Push subscriptions: reminder endpoints, written at runtime
.data/

# Verification screenshots, taken while working — not the repo's business
shots/

# Eval output: results, transcripts and the rendered report are derived
.claude/hillclimb/**/traces/
.claude/hillclimb/**/results.jsonl
.claude/hillclimb/**/errors.jsonl
.claude/hillclimb/**/report.html
.claude/hillclimb/**/trajectory/
eval/nutritionist/cases.html
